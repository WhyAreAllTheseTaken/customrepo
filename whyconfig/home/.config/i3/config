# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Noto Sans Display Medium 14

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- betterlockscreen -l

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

focus_follows_mouse no
focus_on_window_activation none

# start a terminal
bindsym $mod+Return exec /usr/bin/kitty

# kill focused window
bindsym $mod+q kill

# Focus Changing
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move Windows
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
# Video
set $ws0 "0:Vi"
# Search
set $ws1 "1:Se"
# Editor
set $ws2 "2:Ed"
# Build
set $ws3 "3:Bu"
# Game / App
set $ws4 "4:Ga"
# Local Server
set $ws5 "5:Ls"
set $ws6 "6:Lf"
# Production
set $ws7 "7:P1"
set $ws8 "8:P2"
set $ws9 "9:Pf"
# Neofetch/Cava
set $ws10 "10:Ne"
# Discord
set $ws11 "11:Di"
## Steam
set $ws12 "12:St"
# Git
set $ws13 "13:Gh"
# Mail
set $ws14 "14:Ml"
# Music
set $ws15 "15:Mu"
# Tasks
set $ws16 "16:Ta"
# Messages
set $ws17 "17:Ms"
# Timetables
set $ws18 "18:Tt"
# Notes
set $ws19 "19:No"
# Monitor
set $ws20 "20:Mo"
# Virtual Machines
set $ws21 "21:Vs"
set $ws22 "22:Vm"
# Unallocated
set $ws30 "30"
set $ws31 "31"
set $ws32 "32"
set $ws33 "33"
set $ws34 "34"
set $ws35 "35"
set $ws36 "36"
set $ws37 "37"
set $ws38 "38"
set $ws39 "39"
set $ws40 "40"

# switch to workspace
bindsym $mod+grave workspace $ws0
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
bindsym $mod+F1 workspace $ws11
bindsym $mod+F2 workspace $ws12
bindsym $mod+F3 workspace $ws13
bindsym $mod+F4 workspace $ws14
bindsym $mod+F5 workspace $ws15
bindsym $mod+F6 workspace $ws16
bindsym $mod+F7 workspace $ws17
bindsym $mod+F8 workspace $ws18
bindsym $mod+F9 workspace $ws19
bindsym $mod+F10 workspace $ws20
bindsym $mod+F11 workspace $ws21
bindsym $mod+F12 workspace $ws22 
bindsym $mod+Control+grave workspace $ws30
bindsym $mod+Control+1 workspace $ws31
bindsym $mod+Control+2 workspace $ws32
bindsym $mod+Control+3 workspace $ws33
bindsym $mod+Control+4 workspace $ws34
bindsym $mod+Control+5 workspace $ws35
bindsym $mod+Control+6 workspace $ws36
bindsym $mod+Control+7 workspace $ws37
bindsym $mod+Control+8 workspace $ws38
bindsym $mod+Control+9 workspace $ws39
bindsym $mod+Control+0 workspace $ws40

# move focused container to workspace
bindsym $mod+Shift+grave move container to workspace $ws0
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+F1 move container to workspace $ws11
bindsym $mod+Shift+F2 move container to workspace $ws12
bindsym $mod+Shift+F3 move container to workspace $ws13
bindsym $mod+Shift+F4 move container to workspace $ws14
bindsym $mod+Shift+F5 move container to workspace $ws15
bindsym $mod+Shift+F6 move container to workspace $ws16
bindsym $mod+Shift+F7 move container to workspace $ws17
bindsym $mod+Shift+F8 move container to workspace $ws18
bindsym $mod+Shift+F9 move container to workspace $ws19
bindsym $mod+Shift+F10 move container to workspace $ws20
bindsym $mod+Shift+F11 move container to workspace $ws21
bindsym $mod+Shift+F12 move container to workspace $ws22
bindsym $mod+Shift+Control+grave move container to workspace $ws30
bindsym $mod+Shift+Control+1 move container to workspace $ws31
bindsym $mod+Shift+Control+2 move container to workspace $ws32
bindsym $mod+Shift+Control+3 move container to workspace $ws33
bindsym $mod+Shift+Control+4 move container to workspace $ws34
bindsym $mod+Shift+Control+5 move container to workspace $ws35
bindsym $mod+Shift+Control+6 move container to workspace $ws36
bindsym $mod+Shift+Control+7 move container to workspace $ws37
bindsym $mod+Shift+Control+8 move container to workspace $ws38
bindsym $mod+Shift+Control+9 move container to workspace $ws39
bindsym $mod+Shift+Control+0 move container to workspace $ws40

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+l exec betterlockscreen -l blur

# resize window (you can also use the mouse for that)
mode "resize" {
        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Colors
set $bg_color #121212
set $bg_color_light #636363
set $text_color #e0e0e0
set $accent_color #8ab4f8
set $accent_color_dark #4a7bc9
set $urgent_color #ce7276

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks -c ~/.config/i3blocks/i3blocks.conf
	colors {
		background $bg_color
		statusline $text_color
		separator $bg_color_light
		separator $bg_color_light
		focused_workspace $accent_color_dark $accent_color_dark $text_color
		active_workspace $bg_color_light $bg_color $bg_color
		inactive_workspace $bg_color $bg_color $text_color
		urgent_workspace $urgent_color $urgent_color $text_color
	}
}

bindsym $mod+d exec rofi -show run -lines 15 -terminal kitty
bindsym $mod+z exec rofi -show ssh -lines 15 -terminal kitty
bindsym $mod+tab exec rofi -show window -lines 15 -terminal kitty
bindsym $mod+shift+w exec rofi -show drun -lines 15 -terminal kitty

# i3 Gaps
default_border pixel 0
default_floating_border pixel 0
smart_gaps on
smart_gaps inverse_outer

gaps inner 8
gaps outer 8

hide_edge_borders both

# Hide Startup
#exec --no-startup-id feh -xF --zoom fill /usr/share/backgrounds/bg1440.png & sleep 7; pkill feh

# Startup
exec_always --no-startup-id betterlockscreen -u /usr/share/backgrounds/
exec_always --no-startup-id sleep 1;betterlockscreen -w
exec_always --no-startup-id pkill picom; sleep 1; picom -b --config ~/.config/picom.conf
exec --no-startup-id start-pulseaudio-x11
exec --no-startup-id nm-applet
exec_always --no-startup-id flameshot
exec --no-startup-id lxpolkit

# Startup Programs
exec discord
exec /opt/BlueMail/bluemail --disable-gpu-sandbox
# exec chromium --new-window --app=https://soundcloud.com
exec chromium --new-window --app=https://track.toggl.com/timer
exec chromium --new-window --app=https://todoist.com
exec chromium --new-window --app=https://github.com
# exec steam
# exec kitty --name top sudo htop
exec kitty --name edit --directory=~/dev /usr/bin/start_nnn.sh
exec kitty --name build --directory=~/dev /usr/bin/start_nnn.sh
# exec kitty --name server --directory=~/server /usr/bin/start_nnn.sh
# exec kitty --name sfiles --directory=~/server /usr/bin/start_nnn.sh
# exec kitty --name display --hold neofetch
# exec kitty --name display --hold cava
exec obsidian
# exec whatsdesk

# Assignments
assign [class="discord"] $ws11
assign [class="steam"] $ws12
assign [class="whatsdesk"] $ws17
assign [class="googlemessages"] $ws17
assign [class="BlueMail"] $ws14
assign [title="Steam"] $ws12
assign [class="Chromium" instance="soundcloud.com"] $ws15
assign [class="Chromium" instance="toggl.com"] $ws16
assign [class="Chromium" instance="todoist.com"] $ws16
assign [class="Chromium" instance="timetable"] $ws18
assign [class="Chromium" instance="github.com"] $ws13
assign [class="kitty" instance="top"] $ws20
assign [class="kitty" instance="edit"] $ws2
assign [class="kitty" instance="build"] $ws3
assign [class="kitty" instance="server"] $ws5
assign [class="kitty" instance="sfiles"] $ws6
assign [class="kitty" instance="display"] $ws10
assign [class="Virt-manager"] $ws22
assign [title=".*on QEMU/KVM.*"] $ws21
assign [class="obsidian"] $ws19
assign [class="Minecraft"] $ws4
assign [class="worldsim"] $ws4

for_window [class="feh"] floating enable

# Keybinds
bindsym $mod+shift+a exec pavucontrol
bindsym $mod+c exec kitty cava
bindsym $mod+w exec vivaldi --new-window
bindsym $mod+e exec kitty /usr/bin/start_nnn.sh
bindsym $mod+t exec kitty htop
bindsym $mod+j exec eclipse
bindsym $mod+b exec /opt/BlueMail/bluemail --disable-gpu-sandbox
bindsym $mod+g exec gimp
bindsym $mod+shift+d exec onlyoffice-desktopeditors
bindsym $mod+shift+s exec chromium --new-window --app=https://soundcloud.com
bindsym $mod+shift+t exec chromium --new-window --app=https://track.toggl.com/timer
bindsym $mod+control+t exec chromium --new-window --app=https://todoist.com
bindsym $mod+shift+g exec chromium --new-window --app=https://github.com
bindsym $mod+m exec whatsdesk

# Screenshot keybinds
bindsym Print exec flameshot screen
bindsym $mod+s exec flameshot gui
